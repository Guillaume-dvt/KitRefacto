# Kit de départ — Brief « Refactoring en équipe » (P13)

Dépôt modèle à pousser sur GitHub par le formateur (cocher « Template repository »).

- `nettoyage.py` : script fonctionnel mais mal écrit, à refactorer
- `data/ventes.csv` : données brutes (123 lignes, doublons et formats hétérogènes)
- `reference/` : sorties attendues, figées — **ne pas modifier**
- `verifier.py` : compare les sorties du script aux références

```bash
python nettoyage.py && python verifier.py
```
